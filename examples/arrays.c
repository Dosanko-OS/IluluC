/* Gerado por IluluC a partir de examples/arrays.ilc */
#include <stdbool.h>
#include <stdint.h>
#include <stddef.h>
#include <stdio.h>

int main(void);

int main(void) {
    uint8_t buffer[4];
    (buffer[0] = 1);
    (buffer[1] = 2);
    (buffer[2] = 3);
    (buffer[3] = 4);
    return 0;
}

